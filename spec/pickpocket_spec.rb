require 'spec_helper'

describe Pickpocket do
  it 'has a version number' do
    expect(Pickpocket::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
